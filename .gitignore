# Claude Code tooling — installed skill bundles and their lockfile, not project source
.agents/
skills-lock.json

# OS/editor junk
.DS_Store
Thumbs.db

# Secrets — never track. Root .env holds TOKEN_VERCEL; apps/web/.gitignore
# already covers env files under apps/web, this covers the repo root too.
.env
.env.*
!.env.example

# Local QA tooling state
.playwright-cli/

# Vercel CLI local project link (4I2)
.vercel
